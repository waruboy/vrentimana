<h1>Brentimana</h1>

<div class="row">
  <div class="col-lg-12">
    <%= form_tag("/search", method: "get") do %>
      <%= label_tag(:q, "Halte terdekat dari:") %>
      <%= text_field_tag(:q) %>
      <%= submit_tag("Cari") %>
    <% end %>
  </div>
</div>
